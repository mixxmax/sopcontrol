# Product red-team report — 2026-09-02

**Checklist:** `docs/verification/REDTEAM_CHECKLIST.md`  
**When (UTC):** 2026-09-02T07:38Z  
**Scope:** sopcontrol 本仓 + JobsFlow（`ai-job-search`）抽检  
**非目标：** 外部攻击者渗透；本期未新开守卫。

## Results

| # | Item | Verdict | Evidence |
|---|------|---------|----------|
| 1 | 声称核对 | **PASS** | README / LIMITATIONS 仍否定「保证不犯错」；候选不自动写 registry；doctor 下一刀 + harness 差异表仍在 |
| 2 | 已知绕过登记 | **PASS** | `RESIDUAL_RISKS` R1（重命名/动态拼接）、R6（`--no-verify`）仍标明抓不住；未假装已清零 |
| 3 | 节能 | **PASS** | 本仓 doctor 轻量 **0.33s** vs 全量 **1.30s**（~4×）；AGENTS 投影小节 ~**717 tokens** / 62 行（预算 1500/80）；JobsFlow 待人定型 **0**（1288 碎片已拒+归档） |
| 4 | self-test + mutations | **PASS** | self-test：旧入口阻断 / 清洁放行 / 账本篡改阻断；`test_mutations.py` **16 passed**；近期 sim 见 `SIM_20260902T071439Z.md` |
| 5 | 真仓抽检 | **PASS** | JobsFlow：`JF-PREVIEW-001` **pass / wired_and_tested**；doctor 轻量无节能警告；`ambiguity_index=0` |

**Overall: PASS**

## Findings（≤5，非阻塞）

1. **enforced 未到：** `explain` 仍提示未声明 `test_command`、缺运行时 trace——与 LIMITATIONS/手册一致，不是谎报 pass。  
2. **JobsFlow 投影偏肥：** 整份 `AGENTS.md` 仍约 2.5k tokens（含非控制面正文）；控制面小节受预算约束，但真仓全文上下文税仍在——属后续「投影再削 / 用户文档分家」项，非本期失败。  
3. **全量 doctor 在大仓仍贵：** 设计如此（`--full` 显式）；日常须坚持默认轻量。

## Not run / SKIP

- 另一模型完整声称审查（可选；清单 §1 提示词可用）  
- 完整 `./scripts/vertical-check.sh`（本日 self-test + mutations 已覆盖对抗核；vertical-check 先日质检已 564 passed）

## Follow-ups（不要用加守卫完成）

- 实跑 dogfood 周记 `growth diff`  
- 可选：JobsFlow `sopctl test-command --set ...` 朝 E4/enforced  
- 可选：真仓 AGENTS 非控制面正文与投影分家，进一步省 token  
