# SOP Control wrap-up — 2026-09-08

**Branch:** `zcode/living-project-batch1`  
**Base:** `1b086de` (P0/P1 ledger/tickets)  
**Scope:** sopcontrol only; JobsFlow untouched in this wrap-up.

## Fix: `test_gate_result_emits_event`

**Cause:** `run_gate` now prints `len(report.evidence)`; the test stub only had `verdicts=[]`.

**Fix:** Stub uses real `AuditReport` (`evidence`, `verdicts`, `mode`, `coverage`). Added `test_gate_report_contract_requires_evidence_attr` so future stubs cannot drop `evidence`.

**Related:** `run_audit` default mode restored to **`discovery`** (CLI parity). Gate still passes `mode="enforcement"`. Source-unreadable fail-closed only for `source.type in {document, file}` — avoids false fails on `manual_seed` fixtures.

## Full suite

```bash
python -m pytest -q
# 653 passed, 1 skipped
```

## Checklist confirmation

| Area | Status |
|------|--------|
| Ledger O(n) `append_many` + id cache | Confirmed (prior + tests) |
| Gate stage progress | Confirmed |
| Ledger diagnose line numbers | Confirmed |
| discovery ≠ enforcement | Confirmed; default audit=discovery |
| Hook resolver / no `--no-verify` | Confirmed in `HOOK_TEMPLATE` |
| Capability tickets | Confirmed + adapter README |
| Events | Confirmed |

## Commit

See `git log -1` after wrap-up commit (**not pushed**).
